import styled from '@emotion/styled';

const Container = styled.div`
  padding-bottom: 24px;
`;

const ErrorText = styled.p`
  font-size: 20px;
  color: red;
  font-weight: 700;
  text-align: center;
`;

const css = { Container, ErrorText };

export default css;
